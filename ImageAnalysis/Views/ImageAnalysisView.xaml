<UserControl x:Class="ImageAnalysis.Views.ImageAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageAnalysis.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="true"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="0" Margin="10,5,5,5">
            <Button Height="40px" Margin="0,0,0,5" Content="Open First Image" Command="{Binding OpenImageCommand}"/>
            <Button Height="40px" Margin="0,0,0,5" Content="Open Second Image" Command="{Binding OpenSecondImageCommand}"/>
            <Button Height="40px" Margin="0,0,0,5" Content="Substract Images" Command="{Binding SubstractImageCommand}"/>
        </StackPanel>

        <ContentPresenter Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" 
                      Content="{Binding ElementName=imageListView, Path=SelectedItem}">
            <ContentPresenter.ContentTemplate>
                <DataTemplate>
                    <Image Source="{Binding ImageData}"></Image>
                </DataTemplate>
            </ContentPresenter.ContentTemplate>
        </ContentPresenter>

        <ItemsControl Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" ItemsSource="{Binding Spots}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Ellipse Width="{Binding Radius}" Height="{Binding Radius}" Stroke="Red" StrokeThickness="4" Canvas.Left="{Binding Position.X}" Canvas.Top="{Binding Position.Y}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ListView Name="imageListView" Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Margin="5,5,5,5" BorderThickness="0" ItemsSource="{Binding ImgList}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Label x:Name="srcImgLabel" Content="{Binding Title}"/>
                        <Image x:Name="srcImg" Stretch="Fill" Width="100" Source="{Binding ImageData}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
