<UserControl x:Class="PatternAnalysis.Dialogs.PatternCompareDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PatternAnalysis.Dialogs"
             xmlns:converter="clr-namespace:PatternAnalysis.Converter"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:oxy="http://oxyplot.org/wpf"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <converter:HistogramColorSelector x:Key="HistogramColorSelector" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="4*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <oxy:PlotView Name="PatternPlotA" Margin="5" Model="{Binding PlotModelPatternA}" Grid.Row="0" Grid.Column="0" Width="350" Height="350" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <oxy:PlotView Name="PatternPlotB" Margin="5" Model="{Binding PlotModelPatternB}" Grid.Row="0" Grid.Column="1" Width="350" Height="350" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <oxy:PlotView Name="HistoPlot" Model="{Binding PlotModelHisto}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Width="730" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Height="25" Width="100" Margin="5" Content="OK" Command="{Binding CloseDialogCommand}"/>
        <StackPanel  Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Margin="10">
            <Label FontWeight="Bold" Content="Histogram Values" HorizontalAlignment="Center"/>
            <ListView Name="histoTable" BorderThickness="0" ItemsSource="{Binding HistoTable}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel>
                            <TextBlock Foreground="{Binding X, Converter={StaticResource HistogramColorSelector}}" FontWeight="Bold" Text="Bin: " />
                            <TextBlock Foreground="{Binding X, Converter={StaticResource HistogramColorSelector}}" Text="{Binding X}" />
                            <TextBlock Foreground="{Binding X, Converter={StaticResource HistogramColorSelector}}" FontWeight="Bold" Text=" Count: " />
                            <TextBlock Foreground="{Binding X, Converter={StaticResource HistogramColorSelector}}" Text="{Binding Y}" />
                        </WrapPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

    </Grid>
</UserControl>
